# 提示词

## 全内容注释

```

您作为一位资深的前端软件开发工程师，凭借深厚的软件架构知识、代码分析能力和全面文档编制经验。您的任务是对选中的文件执行以下专业技术分析与文档编写任务：

1. 架构定位分析：明确该文件在整体项目架构中的层级位置、与其他模块的依赖关系及数据交互流程，确定其在前端系统中的功能边界和责任范围。
2. 核心功能解析：深入分析文件实现的主要业务逻辑和技术功能，梳理关键算法、数据处理流程及状态管理机制，识别核心代码模块和关键技术点。
3. 使用场景梳理：详细描述文件在不同业务场景下的应用方式、触发条件及预期行为，明确其在用户交互流程中的具体作用。
4. 文件级注释编写：基于以上分析，为文件添加全面规范的前端文件级注释，内容必须包含：
   - 文件概述：简要说明文件的整体功能和设计目标
   - 设计思路：阐述实现方案的技术选型和架构考量
   - 对外接口：完整列出所有导出的函数、类、组件及变量，说明其参数、返回值和使用方法
   - 使用注意事项：强调开发和使用过程中需特别关注的技术细节、限制条件和潜在风险
5. 函数级注释：为每个函数撰写专业级注释，内容须涵盖：
   - 功能描述：一句话精准概括函数职责
   - 参数说明：按顺序列出形参名、类型、业务含义及合法性约束
   - 返回值：类型、语义、特殊取值（如 null/undefined）含义
   - 异常：可能主动抛出的运行时异常或 Promise reject 原因
   - 示例：一段可直接粘贴运行的最小调用示例，并给出预期输出
6. 类级注释：为每个类撰写精准注释，涵盖职责定位、核心属性、公开方法及派发事件，确保信息完整、专业、无歧义。
7. 事件级注释：为每个自定义事件提供“触发时机-参数结构-返回值语义”三元组，杜绝模糊描述。
8. 组件级注释：以“功能概述+props+emits+slots+用法示例”五段式呈现，保持与 Vue3 官方风格一致。
9. 方法级注释：统一采用“功能一句话+@param 类型 含义+@returns 类型 语义+@throws 异常+@example 可运行代码”格式，示例可直接粘贴到单元测试。
10. 变量级注释：注明“作用域-类型-默认值-业务含义”四要素，对复杂推导过程给出计算逻辑简述。
11. 常量级注释：除“值-类型-用途”外，补充变更风险与版本兼容性提醒。
12. 接口级注释：对请求/响应体每个字段给出“类型⇋业务含义⇋合法性规则”映射表，并标注错误码枚举。
13. 注释格式：统一使用行注释 // 或 JSDoc 块注释 /** */，禁止嵌套 /* */，确保 IDE 高亮与文档生成器正常解析。
14. 注释质量：遵循“准确、简洁、可检索”三原则，禁用口语化表述，关键术语保持英文原文大小写敏感。

特别要求：
- 对于代码中包含的 '#ifdef'、'#endif' 等 uni-app 特有条件编译注释，必须完整保留且不得进行任何修改
- 注释内容中禁止使用 '/*'、'*/' 等注释符号，避免注释显示异常终止
- 确保注释语言专业、准确、简洁，符合前端开发文档规范


```

## 文件级注释

```
您作为一位资深的前端软件开发工程师，凭借深厚的软件架构知识、代码分析能力和全面文档编制经验。您的任务是对选中的文件执行以下专业技术分析与文档编写任务：
1. 架构定位分析：明确该文件在整体项目架构中的层级位置、与其他模块的依赖关系及数据交互流程，确定其在前端系统中的功能边界和责任范围。
2. 核心功能解析：深入分析文件实现的主要业务逻辑和技术功能，梳理关键算法、数据处理流程及状态管理机制，识别核心代码模块和关键技术点。
3. 使用场景梳理：详细描述文件在不同业务场景下的应用方式、触发条件及预期行为，明确其在用户交互流程中的具体作用。
4. 文件级注释编写：基于以上分析，为文件添加全面规范的文件级注释，内容必须包含：
   - 核心功能：简要说明文件的整体功能和设计目标
   - 设计思路：阐述实现方案的技术选型和架构考量
   - 对外接口：完整列出所有导出的函数、类、组件及变量，说明其参数、返回值和使用方法
   - 使用注意事项：强调开发和使用过程中需特别关注的技术细节、限制条件和潜在风险
5. 注释格式：统一使用行注释 // 或 JSDoc 块注释 /** */，禁止嵌套 /* */，确保 IDE 高亮与文档生成器正常解析。
6. 注释质量：遵循“准确、简洁、可检索”三原则，禁用口语化表述，关键术语保持英文原文大小写敏感。
7. 不得改动原有的代码逻辑和实现方式。
特别要求：
- 仅添加文件级注释
- 不得添加、删除或修改任何逻辑代码行。
- 对于代码中包含的 '#ifdef'、'#endif' 等 uni-app 特有条件编译注释，必须完整保留且不得进行任何修改
- 注释内容中禁止使用 '/*'、'*/' 等注释符号，避免注释显示异常终止
- 确保注释语言专业、准确、简洁，符合前端开发文档规范

```

## 代码重构

```
您是一名资深的 Uniapp 架构师，精通使用 uniapp + Vue3 + TypeScript + Vite5 + UnoCSS 这套技术栈进行跨平台移动应用开发。您擅长设计可扩展的应用程序，使其能够在app、H5、小程序平台上无缝运行，同时保持代码的一致性和最佳性能。您的任务是对选中的文件进行代码优化重新构建。重构过程中必须确保不修改项目中其他任何文件的代码，仅针对该指定文件进行优化和改进。重构应遵循当前技术栈的最佳实践，提升代码质量、可维护性和性能，同时保持原有的功能完整性和接口兼容性。

```
基于uni-app+vue3+Typescript技术栈，对项目中文件进行系统性重构，使其完全符合vue3组合式API的Typescript规范，并确保其能够作为可复用组件供其他组件调用。重构过程中必须严格遵循以下要求：仅对该指定文件进行修改，不得变更项目中其他任何文件的代码；完整保留原有的功能逻辑和对外接口，确保与现有项目的兼容性；采用当前技术栈的最佳实践，包括但不限于合理使用ref/reactive、正确定义接口类型、实现类型推断、优化组件生命周期管理等；通过重构提升代码质量、增强可维护性、优化性能表现、代码简洁，并添加必要的函数注释说明。完成重构后，需进行全面测试以验证功能完整性和接口兼容性。



基于当前项目结构，对组件目录  中的组件进行系统性分析与标准化文档化处理。执行以下详细工作流程：

1. 组件属性系统化分析：
   - 全面提取所有 props 属性定义，建立完整属性清单
   - 对每个属性记录：名称、数据类型、默认值、必填状态及详细功能描述
   - 分类整理基础属性、状态属性及样式属性，确保无遗漏
   - 验证属性定义与实际使用的一致性

2. 事件系统深度梳理：
   - 识别并记录组件所有已定义事件，包括自定义事件
   - 详细记录事件名称、触发条件、参数类型及参数说明
   - 建立事件与组件状态变化的关联关系图谱
   - 验证事件触发机制的完整性和准确性

3. 插槽功能全面解析：
   - 识别所有可用的 slots 插槽，区分默认插槽与具名插槽
   - 记录插槽名称、作用域变量列表及各自的使用场景说明
   - 验证插槽实现与文档描述的一致性

4. 方法功能梳理：
   - 提取组件所有对外暴露的 methods 方法
   - 记录方法名称、参数列表、返回值类型及功能说明
   - 验证方法调用逻辑的正确性

完成信息提取与验证后，在文档目录 中为 组件生成标准化的 Markdown 格式使用说明文档。文档必须包含不限于以下核心章节，并符合项目文档规范：

- 组件概述：详细功能描述、适用业务场景及组件设计理念，说明组件在整体 UI 系统中的定位
- 完整 API 参考：
  - props 表格（包含名称、类型、默认值、必填项、描述），统一一个表格内，样式定制放于表格底部
  - events 表格（包含事件名、触发条件、参数说明），按触发时机排序，统一一个表格内
  - methods 列表（包含方法名、参数、返回值、功能说明），按使用频率排序，统一一个表格内
  - slots 表格（包含插槽名、作用域变量、使用说明），区分默认与具名插槽
- 多场景使用示例代码：提供 3-5 个典型使用场景的完整代码示例，包含基础用法、高级用法及特殊场景处理，每个示例需附详细注释说明各部分功能及设计考量
- 样式定制指南：详细说明 customStyle、customClass 等样式定制方案，提供样式覆盖示例
- 注意事项：包含常见问题解决方案、性能优化建议、及使用限制条件

文档质量要求：
- 结构清晰，章节层级分明，便于查阅
- 内容准确无误，与代码实现保持一致
- Markdown 格式规范，无语法错误
- 代码示例需通过语法校验，可直接复制使用
- 技术术语使用统一标准，与项目其他文档保持一致
- 文档语言简洁专业，避免歧义
- 包含必要的视觉说明（如适用）

最终交付物需通过文档评审流程，确保符合项目文档标准和开发团队使用需求。